
<h1>Прокат.</h1>
<h2>@Model.currentCategory</h2>
<div class="row mt-5 mb-2">
    @{
        foreach (RentItem rentItem in Model.GetAllRentItems)
        {
            @Html.PartialAsync("RentItemsTemplate",rentItem)
        }
    }
</div>
