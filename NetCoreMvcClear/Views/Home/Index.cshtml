@model HomeViewModel

<h1>Прокат. Доступно.</h1>
<div class="row mt-5 mb-2">
    @{
        foreach (RentItem rentItem in Model.availableItems)
        {
            @Html.Partial("RentItemsTemplate", rentItem)
        }
    }
</div>